version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - deno_cache:/root/.deno
    restart: unless-stopped

volumes:
  deno_cache:
